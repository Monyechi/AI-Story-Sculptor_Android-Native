package com.monyechi.aistorysculptor.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary brand greens (from dashboard-container, module, book-header) ──
val DarkForestGreen = Color(0xFF2C3F2C)
val MediumForestGreen = Color(0xFF345232)

// ── Accent / olive tones ──
val DarkOlive = Color(0xFF474321)
val OliveAccent = Color(0xFF9E9F55)
val YellowGreen = Color(0xFFABAD60)

// ── Earth / neutral tones (sidebar, scrollable-div, inputs) ──
val Beige = Color(0xFFD1C8A1)
val BeigeAlpha90 = Color(0xE5D1C8A1)   // #d1c8a1e5
val LightSage = Color(0xFFBBBEA0)
val WarmCream = Color(0xFFF8F5E9)       // --light from book detail
val LightCream = Color(0xFFF4EBE4)

// ── Status / action colors ──
val SuccessGreen = Color(0xFF4CAF50)
val AccentGreen = Color(0xFF66C166)     // --accent from book detail
val Coral = Color(0xFFE0746C)           // create book submit
val DangerRed = Color(0xFFFF6B6B)
val WarningYellow = Color(0xFFF7C948)
val Gold = Color(0xFFB8994A)

// ── Neutral / text colors ──
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
val DarkText = Color(0xFF333333)
val GrayBody = Color(0xFF818992)

// ── Overlay colors ──
val OverlayDark = Color(0x80000000)     // rgba(0,0,0,0.5)
